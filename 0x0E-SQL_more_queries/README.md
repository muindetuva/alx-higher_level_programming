# SQL More Queries

This project extends the introductory SQL work with users and privileges,
column constraints, primary and foreign keys, subqueries, inner and outer
joins, grouping, and aggregate ratings. Its scripts build the USA state/city
schema and query the supplied television-show datasets, including the advanced
genre-exclusion and rating reports.
